# 6 — Effectuer un swap

`zeroForOne` choisit le sens. `amountSpecified` négatif signifie entrée exacte ; positif, sortie exacte.

`Swap.sol` utilise `PoolSwapTest` : routeur de test, impropre à la production.
