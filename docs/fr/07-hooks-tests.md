# 7 — Frais, hooks et tests

Les frais sont définis dans `PoolKey`. Les hooks ajoutent une logique autour des actions du pool.

```sh
forge test
git log --oneline
git switch --detach <commit>
```

Revenir ensuite avec `git switch gh-pages`.
