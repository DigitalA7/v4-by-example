# 4 — Initialiser un pool

`PoolKey` identifie un pool par deux monnaies triées, les frais, l'espacement des ticks et le hook.

`PoolManager.initialize()` reçoit aussi le prix initial `sqrtPriceX96` et les données du hook.

Voir `src/pages/initialize/PoolInitialize.sol`.
